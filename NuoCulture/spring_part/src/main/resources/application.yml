#spring.datasource.url=jdbc:mysql://localhost:3306/nuoculture?serverTimezone=GMT%2B8
#spring.datasource.username=root
#spring.datasource.password=123456
#spring.datasource.driver-class-name=com.mysql.jdbc.Driver
#server.port=8090

server:
  port: 8090
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nuoculture?characterEncoding=utf-8&serverTimezone=GMT%2B8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      enabled: true
      max-file-size: 2048MB
      max-request-size: 2048MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    auto-mapping-behavior: full
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

  #    mapper-locations: classpath*:/**/mapper/**/*.xml
  type-aliases-package: com.tfq.dao
  global-config:
    db-config:
      logic-delete-field: is_delete
      logic-delete-value: 1 # 逻辑已删除值(默认为 1)
      logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)

file:
  staticPath: http://localhost:8090/